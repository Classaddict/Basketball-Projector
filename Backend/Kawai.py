import pandas as pd
import numpy as np


class Kawai:
    @staticmethod 
    def load_data():
        """
            Takes in the data from the player data database and uses it as data 
            for the machine 
        """

    @staticmethod
    def add_features():
        """
            uses the data to add into the dataframe 
        """

    @staticmethod
    def learn():
        """
            Builds set and creates target variable, 
            Splits data into features(x) and targets(y) for the model 
        """

    @staticmethod
    def predict():
        """
            Trains the model using chronological split 
            -first 80% into training, last 20% into testing, 
        """ 