import {Component, Card, CardTitle } from "react";

export default class header extends Component{
    
    render(){
        return(
            <Card>
                <CardTitle>
                    Basketball Projections
                </CardTitle>
            </Card>
        );
    }
}